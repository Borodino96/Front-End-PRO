let hours = prompt ('укажите количество часов для конвертации в секунды', '');
let oneHour = 60*60;
let x;
if (x = hours ) {
  let result = x*oneHour;
  alert(result)
}
else {
  alert('введите количество часов для рассчета')
}